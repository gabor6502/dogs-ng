import { Component } from '@angular/core';

@Component({
  selector: 'app-collected',
  imports: [],
  templateUrl: './collected.html',
  styleUrl: './collected.scss'
})
export class Collected 
{
  // empty

  // I think that the spinner and collected must be combined into one component
  // this way they can acess each other's data better, in a more straightforward, less convoluted way
}
